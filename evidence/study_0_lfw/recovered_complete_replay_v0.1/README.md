# Recovered complete Study 0 MMALS replay — recovery record

This directory records the recovery identity and preservation state of the complete historical Study 0 MMALS replay bundle for:

`lfw_resnet18_siamese_projection_v0_1-89179914-911192ee-64559cbd`

Recovered archive:

`lfw-resnet18-siamese-projection-v0.1-mmals-replay.zip`

Verified recovery identity:

- bytes: `4,158,610`
- SHA-256: `7429c75a7da827281ca172d7a4184c65fcc27dbfa845eb9ffd27e04d81331897`
- ZIP members: `40`
- `test_pair_scores.csv` present with `1,821,547` bytes
- `run_manifest.json`, `thresholds.csv`, and `paired_noninferiority.csv` present

The recovered ZIP SHA-256 matches the release checksum that had already been preserved in `SHA256SUMS(2).txt` before this recovery event.

## Durable preservation

A private durable backup of the exact recovered binary was created as an attachment in a Gmail draft with subject:

`ARCHIVE — Study 0 historical MMALS replay recovered 2026-08-19`

This repository intentionally records the identity and recovery provenance rather than copying the binary into normal Git history. The binary backup is independent of temporary ChatGPT sandbox paths.

## Scientific boundary

Recovery verification was byte/provenance-only:

- historical score values were not interpreted;
- no scientific claim or gate was promoted;
- Study 1 was not started;
- this record is preservation metadata, not outcome evidence.

The recovered complete replay is the admissible historical input candidate for the already-reviewed corrected Study 0 reanalysis runner, subject to its existing preflight and immutable-input guards.
